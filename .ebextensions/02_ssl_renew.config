files:
  "/etc/cron.d/certbot":
    mode: "000644"
    owner: root
    group: root
    content: |
      0 0,12 * * * root python -c 'import random; import time; time.sleep(random.random() * 3600)' && certbot renew 